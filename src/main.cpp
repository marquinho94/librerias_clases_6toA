#include <Arduino.h>
#include <mi_libreria.h>

// put function declarations here:

int a=3, b=4, c=5, result;

void setup() {
  
  Serial.begin(9600);
}

void loop() {
  int result;
  result= suma_lib(10,15);
  Serial.println(result);
  switch (expression)
  {
  case /* constant-expression */:
    if(lecturaYcomparacion())
    {estado=estadonuevo}
    estado=mismo estado;
    break;
  
  default:
    break;
  }
}


// put function definitions here:
